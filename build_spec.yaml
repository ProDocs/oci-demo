version: 0.1
component: build
timeoutInSeconds: 3600
runAs: root
shell: bash

env:
  variables:
    AI_REVIEW_MODE: "mock"
    AI_REVIEW_SCENARIO: "auto"
    OCI_GENAI_AUTH_MODE: "resource_principal"
    JAVA_HOME: "/usr/lib64/graalvm/graalvm-java21"
  exportedVariables:
    - AI_REVIEW_RESULT
    - BUILD_OUTCOME
    - ARTIFACT_VERSION

steps:
  - type: Command
    name: "Prepare workspace"
    command: |
      set -euo pipefail
      cd "${OCI_PRIMARY_SOURCE_DIR}"
      echo "Working directory: $(pwd)"
      mkdir -p dist
      export ARTIFACT_VERSION="run-$(date -u +%Y%m%d%H%M%S)"
      echo "ARTIFACT_VERSION=${ARTIFACT_VERSION}"
      python3 --version
      python3 -c 'import importlib.util; print("oci-sdk=" + ("present" if importlib.util.find_spec("oci") else "missing"))'

  - type: Command
    name: "AI architecture review"
    command: |
      set -euo pipefail
      cd "${OCI_PRIMARY_SOURCE_DIR}"
      echo "Working directory: $(pwd)"
      test -f docs/architecture-guidelines.md
      test -d src/main/java
      test -f tools/ai_review/main.py
      python3 -m tools.ai_review.main \
        --guidelines docs/architecture-guidelines.md \
        --source-dir src/main/java \
        --output ai-review.json
      cp ai-review.json dist/ai-review.json
      cat ai-review.json
      export AI_REVIEW_RESULT="$(python3 -c 'import json, sys; print(json.load(open(sys.argv[1], encoding="utf-8"))["result"])' ai-review.json)"

  - type: Command
    name: "Gate native build"
    command: |
      set -euo pipefail
      cd "${OCI_PRIMARY_SOURCE_DIR}"
      bash scripts/gate-ai-review.sh ai-review.json
    onFailure:
      - type: Command
        name: "Package blocked artifacts"
        command: |
          set -euo pipefail
          cd "${OCI_PRIMARY_SOURCE_DIR}"
          export BUILD_OUTCOME=BLOCKED
          bash scripts/package-demo-bundle.sh "${BUILD_OUTCOME}"

  - type: Command
    name: "Install GraalVM 21"
    command: |
      set -euo pipefail
      cd "${OCI_PRIMARY_SOURCE_DIR}"
      cat /etc/os-release
      yum repolist
      yum repolist all | grep -E 'ol8_codeready_builder|ol8_oci_included' || true
      yum list "graalvm-21*" || true
      yum -y install \
        --enablerepo=ol8_codeready_builder \
        gcc \
        glibc-devel \
        glibc-static \
        libstdc++-static \
        zlib-devel \
        zlib-static
      yum -y install \
        --enablerepo=ol8_codeready_builder \
        graalvm-21-native-image
    onFailure:
      - type: Command
        name: "Package artifacts after GraalVM install failure"
        command: |
          set -euo pipefail
          cd "${OCI_PRIMARY_SOURCE_DIR}"
          export BUILD_OUTCOME=GRAALVM_SETUP_FAILED
          bash scripts/package-demo-bundle.sh "${BUILD_OUTCOME}"

  - type: Command
    name: "Set GraalVM PATH"
    command: |
      set -euo pipefail
      cd "${OCI_PRIMARY_SOURCE_DIR}"
      export PATH="${JAVA_HOME}/bin:${PATH}"
      java -version
      native-image --version
    onFailure:
      - type: Command
        name: "Package artifacts after GraalVM setup failure"
        command: |
          set -euo pipefail
          cd "${OCI_PRIMARY_SOURCE_DIR}"
          export BUILD_OUTCOME=GRAALVM_SETUP_FAILED
          bash scripts/package-demo-bundle.sh "${BUILD_OUTCOME}"

  - type: Command
    name: "Build native executable"
    command: |
      set -euo pipefail
      cd "${OCI_PRIMARY_SOURCE_DIR}"
      export PATH="${JAVA_HOME}/bin:${PATH}"
      mvn -version
      mvn -Pnative -DskipTests package
    onFailure:
      - type: Command
        name: "Package artifacts after native build failure"
        command: |
          set -euo pipefail
          cd "${OCI_PRIMARY_SOURCE_DIR}"
          export BUILD_OUTCOME=NATIVE_BUILD_FAILED
          bash scripts/package-demo-bundle.sh "${BUILD_OUTCOME}"

  - type: Command
    name: "Package demo artifacts"
    command: |
      set -euo pipefail
      cd "${OCI_PRIMARY_SOURCE_DIR}"
      export BUILD_OUTCOME=SUCCESS
      bash scripts/package-demo-bundle.sh "${BUILD_OUTCOME}"

outputArtifacts:
  - name: ai-review-json
    type: BINARY
    location: dist/ai-review.json
  - name: native-binary
    type: BINARY
    location: target/oci-ai-review-demo
  - name: demo-bundle
    type: BINARY
    location: dist/demo-bundle.zip
